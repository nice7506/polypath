{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node backend for the polypath monorepo",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "dev": "nodemon --watch src --ext ts --exec \"npm run build && node dist/index.js\"",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"No tests yet\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@e2b/code-interpreter": "^2.2.0",
    "@google/generative-ai": "^0.24.1",
    "@supabase/supabase-js": "^2.84.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "typescript": "^5.6.3"
  }
}
